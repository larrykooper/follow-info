# A deleted tagging is a user-tag pair that is gone from Twitter, because either I removed the user from the list, they blocked themself from the list, or the user's account was cancelled.

class DeletedTagging < ActiveRecord::Base 
  
end